package modelo;

public abstract class ProductoMenu extends ProductoVendible {

	public ProductoMenu(String nombre, double precio, boolean disponible)
    {
        super(nombre, precio, disponible);
    }
}
